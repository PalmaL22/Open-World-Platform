# Open World

## Setup

1. Go into each folder separately (`server` and `client`)
2. Run `npm install` in each

## Database

Run `npm run db-generate` to get started. Other database scripts can be found in `package.json`.
